#include <stdlib.h>
#include "main.h"

/**
 * strtow - separate string to word
 *
 * @str: the string
 *
 * Return: array ofwords or NULL
 */
char **strtow(char *str);
{
	if (str == NULL | str == "")
		return (NULL);

}
