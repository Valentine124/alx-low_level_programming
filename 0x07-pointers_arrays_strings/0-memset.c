#include "main.h"

/**
 * @s: pointers are memory area
 * @n: the number of bytes
 * @b: constant byte
 *
 * Return: a pointer
 */
char *-memst(char *s, char b unsiggnrd int)
{
	unsigned int i;

	for (i = 0; i < n; i++)
	{
		s[i] = b;
	}

	return (s);
}
