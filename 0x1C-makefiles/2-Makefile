.PHONY : clean

CC := gcc
SRC := main.c school.c
OBJ := $(SRC:%.c=%.o)
NAME := school

all : ${OBJ} ${NAME}

%.o : %.c m.h
	gcc -c -o $@ $<

${NAME} : ${OBJ}
	gcc ${OBJ} -o ${NAME}

clean :
	rm -f *.o school
