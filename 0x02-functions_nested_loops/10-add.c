#include "main.h"

/**
 * add - adds two integers
 * @f: first operand
 * @s: second operand
 * Return: the sum of the two operands
 */
int add(int f, int s)
{
	int sum;

	sum = f + s;

	return (sum);
}
