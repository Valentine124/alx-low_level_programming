#include "main.h"

/**
 * print_last_digit - output last digit of a number
 * @n: the number in int
 * Return: the last digit
 */
int print_last_digit(int n)
{
	int last;
	
	last = n % 10;
	
	return (last);
}
